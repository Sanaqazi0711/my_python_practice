# Quesstion : CAn you change a value inside a list which is contained in set S?
# s = {8, 7, "harry", [1,2]}

s = {8, 7, "harry", [1,2]}

# set does not support indexing
#cannot include list inside a set
